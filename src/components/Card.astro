---
interface Props {
	imagen?: string;
	titulo?: string;
	github?: string;
	body?: string;
	href?: string;
	leng?: string;
	
}

var lenguajes: string[] = [];

const { href, titulo, body, imagen,github,leng } = Astro.props;


if(leng){
	lenguajes = leng.split(',');
}

let url: string = '';

---
<script>
</script>

<div class="carta d-flex jusitfy-content-center flex-column">
	
	<h4>{titulo}</h4>
	<img src={imagen} alt="">
	<p class="py-3">Tecnologias Utilizadas:</p>
	<div class="carta-tecn">
		{
			lenguajes.map((leng) => {
					const url = `https://skillicons.dev/icons?i=${leng}`;
					return (
						<div class="tecnologia"><span><img src={url} alt="" class="imagen-tec"/> {leng}</span></div>
					);
				})
		}
	</div>
	<div class="botones-card">
		<a href=""><button class="btns-card"><i class="fas fa-code"></i> Codigo</button></a>
		<a href=""><button class="btns-card"><i class="fas fa-code"></i> Ver Pagina</button></a>
		

	</div>
</div>

<style>
	 .botones-card {
		margin-top: 70px;
    }

	.btns-card{
		margin: 5px;
		border: none;
		padding: 8px;
		border-radius: 50px;
		transition: 0.3s;
	}

	.btns-card:hover{
		transform: scale(1.08);
	}

	.imagen-tec{
		width: 15px !important;
		height: 15px !important;
	}

	.carta-tecn{
		display: flex;
		height: 50px;
		padding: 0px;
		flex-wrap: wrap;
	}

	.carta{
		width: 350px;
		background-color: rgb(46, 46, 163);
		border-radius: 15px;
		color: #fff;	
		height: auto; 
		margin: 10px;
		padding: 25px;
		border: 1px solid #fff;
		cursor: pointer;
	}

	h4{
		font-weight: 555;
		margin-bottom: 10px;
	}
	
	h5{
		font-size: 20px;
	}

	.carta img{
		width: 100%;
		border-radius: 10px;
		height: 150px;
		margin-bottom: 5px;
	}


	.tecnologia{
		background-color: #4a37b8;
		padding: 8px;
		border-radius: 10px;
		height: 40px;
		margin: 3px;
	}
	
	.tecnologia img{
		margin-right: 5px;
	}


</style>
